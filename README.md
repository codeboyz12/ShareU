# 📚 Smart Borrow System (ระบบยืม-คืนอุปกรณ์การเรียน)

**Smart Borrow System** คือโปรแกรมจำลองระบบบริหารจัดการการยืม-คืนครุภัณฑ์หรืออุปกรณ์การเรียนสำหรับนักศึกษาและเจ้าหน้าที่ พัฒนาด้วยภาษา **Java** (Pure Java) และใช้ **Swing Framework** ในการสร้างหน้าจอ (GUI) โดยเน้นการออกแบบโครงสร้างตามหลักการ **Object-Oriented Programming (OOP)** อย่างครบถ้วน

## ✨ ฟีเจอร์หลัก (Key Features)

* **ระบบสมาชิก (Authentication):** รองรับการสมัครสมาชิกใหม่ (Register) และการเข้าสู่ระบบ (Login) แยกสิทธิ์ระหว่างนักศึกษาและผู้ดูแลระบบ
* **สำหรับนักศึกษา (Student):**
    * ดูรายการอุปกรณ์ทั้งหมดและสถานะปัจจุบัน (ว่าง/ถูกยืม)
    * ส่งคำร้องขอยืมอุปกรณ์ (Borrow Request)
    * ตรวจสอบสถานะการจอง (Waiting Approval)
* **สำหรับผู้ดูแลระบบ (Admin):**
    * อนุมัติคำร้องการขอยืม (Approve Request)
    * ดำเนินการรับคืนของ (Return Process)
    * **ระบบคำนวณค่าปรับอัตโนมัติ** (Fine Calculation) หากมีการคืนเกินกำหนด (วันละ 100 บาท)
* **Data Storage:** ใช้ ArrayList ในการเก็บข้อมูลแบบ In-Memory (ข้อมูลจะรีเซ็ตเมื่อปิดโปรแกรม)

---

## 🛠️ ความต้องการของระบบ (Prerequisites)

* **Java Development Kit (JDK):** เวอร์ชั่น 8 ขึ้นไป (แนะนำ JDK 17 หรือ 21)
* **Text Editor/IDE:** VS Code, IntelliJ IDEA, หรือ Notepad

---

## 🚀 วิธีการติดตั้งและรันโปรแกรม (Installation & Execution)

### 1. เตรียมไฟล์
ตรวจสอบให้แน่ใจว่าไฟล์ Source Code มีชื่อว่า `SmartBorrowSystem.java` (ตัวพิมพ์เล็ก-ใหญ่ต้องตรงกันเป๊ะ)

### 2. การคอมไพล์ (Compile)
เปิด Terminal หรือ Command Prompt (CMD) ในโฟลเดอร์ที่เก็บไฟล์ แล้วพิมพ์คำสั่ง:

```bash
javac SmartBorrowSystem.java
```

### 3. การรันโปรแกรม (Run)
พิมพ์คำสั่งเพื่อเริ่มโปรแกรม:

```bash
java SmartBorrowSystem
```

---

## 🔐 ข้อมูลสำหรับทดสอบ (Demo Credentials)

* โปรแกรมได้เตรียมข้อมูลจำลอง (Mock Data) ไว้ให้แล้ว สามารถใช้ล็อกอินได้ทันทีโดยไม่ต้องสมัครใหม่

* **👨‍💼 ผู้ดูแลระบบ (Admin)**
    * Username: admin
    * Password: admin
    * หน้าที: อนุมัติการยืม และกดรับคืนของเพื่อดูการคำนวณค่าปรับ
* **🎓 นักศึกษา (Student)**
    * Username: S66001
    * Password: 1234
    * หน้าที: เลือกดูของและกดขอยืม
* (หรือสามารถกดปุ่ม Register New Student เพื่อสมัครสมาชิกใหม่และทดลอง Login เองได้)